import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { API } from '../APIs/routes';
// console.log('routes', API.getLots);

export default function Lots() {
  return <h1>{API}</h1>;
}
